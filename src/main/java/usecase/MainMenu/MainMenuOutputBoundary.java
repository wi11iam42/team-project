package usecase.MainMenu;

public interface MainMenuOutputBoundary {

    /**
     * Presents the main menu data to the user interface.
     *
     * @param data the processed main menu data to be displayed
     */
    void present(MainMenuOutputData data);
}
